# 🚀 網頁自動化任務執行器

這個專案提供了一個簡單的網頁界面，用於執行 Playwright 自動化腳本。

## 📋 功能說明

可以透過網頁界面執行以下腳本：
- **Cyberbiz 訂單爬取** (`run-Cyberbiz.js`) - 自動從 Cyberbiz 平台爬取訂單數據
- **LinePay 交易下載** (`run-linepay.js`) - 自動從 LinePay 平台下載交易記錄

## 🚀 快速開始

### 方式 1：使用批處理檔案（Windows）
```bash
雙擊 start-web.bat
```

### 方式 2：使用命令行
```bash
# 首次使用，需要安裝依賴
npm install

# 啟動服務器
npm start
```

或者

```bash
node server.js
```

## 🌐 訪問界面

啟動服務器後，在瀏覽器中打開：
```
http://localhost:3000
```

## 📦 依賴

- **Node.js** - JavaScript 運行環境
- **Express.js** - Web 框架
- **Playwright** - 瀏覽器自動化工具

## 🎯 使用步驟

1. 確保 `credentials-cyberbiz.txt` 和 `credentials-linepay.txt` 已正確配置
2. 啟動網頁服務器
3. 打開瀏覽器訪問 `http://localhost:3000`
4. 點擊「開始執行」按鈕運行相應的腳本
5. 狀態欄會顯示腳本是否正在運行
6. 點擊「停止」按鈕可隨時終止執行

## 📊 功能詳情

### API 端點

#### 1. 執行腳本
```
POST /api/run-script
Body: { "scriptName": "run-Cyberbiz.js" | "run-linepay.js" }
```

#### 2. 停止腳本
```
POST /api/stop-script
Body: { "scriptName": "run-Cyberbiz.js" | "run-linepay.js" }
```

#### 3. 獲取狀態
```
GET /api/status
Response: { "run-Cyberbiz.js": "running|stopped", "run-linepay.js": "running|stopped" }
```

## 🔧 項目結構

```
playwright/
├── server.js           # Web 服務器主文件
├── public/
│   └── index.html      # 前端網頁界面
├── run-Cyberbiz.js     # Cyberbiz 爬蟲腳本
├── run-linepay.js      # LinePay 爬蟲腳本
├── package.json        # 項目配置
├── start-web.bat       # Windows 快速啟動
└── README.md          # 本文件
```

## 🐛 故障排除

### 1. "Port 3000 already in use"
已有其他進程佔用 3000 端口。可以修改 `server.js` 中的 `PORT` 變數。

### 2. "Cannot find module 'express'"
需要安裝依賴：
```bash
npm install
```

### 3. 腳本執行失敗
- 確保 Node.js 已正確安裝
- 檢查 `credentials-*.txt` 文件是否存在且格式正確
- 查看瀏覽器控制台的錯誤信息

## 📝 筆記

- Web 界面會每 2 秒自動刷新一次運行狀態
- 同一時刻只能運行一個腳本實例
- 腳本的詳細輸出會打印在服務器控制台

## 📞 支持

如有問題，請檢查：
1. Node.js 版本（建議 v14.0.0 或以上）
2. credentials 配置文件是否正確
3. 防火牆設置是否允許訪問 localhost:3000

---
**版本**: 1.0.0 | **更新時間**: 2026-01-27
